{
  "name": "midnight-dapps",
  "version": "1.0.0",
  "private": true,
  "description": "Experimental sample projects on top of Midnight using Compact",
  "main": "index.js",
  "scripts": {
    "prepare": "is-ci || husky",
    "compact": "turbo run compact",
    "compact:fast": "turbo run compact -- --skip-zk",
    "compact:version": "turbo run compact -- --version",
    "compact:language-version": "turbo run compact -- --language-version",
    "build": "pnpm -r --workspace-concurrency=1 run build",
    "build:contracts": "pnpm -r --filter './contracts/*' --workspace-concurrency=1 run build",
    "build:apps": "pnpm -r --filter './apps/*' --workspace-concurrency=1 run build",
    "build:packages": "pnpm -r --filter './packages/*' --workspace-concurrency=1 run build",
    "compile:contracts": "pnpm -r --filter './contracts/*' --workspace-concurrency=1 run compact",
    "tsc:contracts": "pnpm -r --filter './contracts/*' --workspace-concurrency=1 run tsc",
    "test:contracts": "turbo run test --filter './contracts/*'",
    "test:apps": "turbo run test --filter './apps/*'",
    "fmt": "turbo run fmt",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "types": "pnpm compact:fast && turbo run types",
    "precommit": "turbo run precommit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.4.1",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "@commitlint/types": "^19.8.1",
    "@midnight-ntwrk/compact-runtime": "^0.8.1",
    "@tailwindcss/vite": "^4.1.11",
    "@types/node": "^22.15.29",
    "@vitest/coverage-v8": "^3.1.4",
    "husky": "^9.1.7",
    "is-ci": "^4.1.0",
    "lint-staged": "^16.1.2",
    "tailwindcss": "^4.0.6",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.1.4"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "@biomejs/biome",
      "esbuild"
    ]
  },
  "engines": {
    "node": "22.x"
  },
  "engineStrict": true
}
