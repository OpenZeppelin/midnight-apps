name: PR Lifecycle

on:
  pull_request:
    types: [opened, reopened, converted_to_draft, ready_for_review, closed]

jobs:
  pr-lifecycle:
    uses: OpenZeppelin/midnight-handbook/.github/workflows/midnight-pr-lifecycle.yml@main
    secrets:
      PROJECT_TOKEN: ${{ secrets.PROJECT_TOKEN }}
