// SPDX-License-Identifier: MIT
// OpenZeppelin Midnight Apps Contracts v0.0.1-alpha.2 (lunarswap/test/mocks/LunarswapPair.mock.compact)

pragma language_version >= 0.19.0;

import {
  ShieldedCoinInfo,
} from CompactStandardLibrary;

import { Pair } from "../../../types/TPair";

import {
  initializePair
} from "../../../LunarswapPair" prefix LunarswapPair_;

export { Pair, ShieldedCoinInfo };

// Helper for test suite: toggling this variable activates circuit metadata reporting.
ledger _isCircuitInfoExposed: Boolean;

export circuit initializePair(
  id: Bytes<32>,
  token0: ShieldedCoinInfo,
  token1: ShieldedCoinInfo
): Pair {
  _isCircuitInfoExposed = true;
  return LunarswapPair_initializePair(disclose(id), token0, token1);
}
