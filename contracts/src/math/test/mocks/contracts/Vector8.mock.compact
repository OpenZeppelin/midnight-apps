// SPDX-License-Identifier: MIT
// OpenZeppelin Midnight Apps Contracts v0.0.1-alpha.0 (math/test/Vector8.mock.compact)

pragma language_version >= 0.20.0;

import CompactStandardLibrary;

import "../../../Vector8" prefix Vector8_;

// Helper for test suite: hardcoded to true in every circuit to enable circuit metadata reporting; only increases circuit size by 3 rows per exposed circuit.
ledger toImpure: Boolean;

export circuit toUint64(vec: Vector<8, Uint<8>>): Uint<64> {
  toImpure = true;
  return Vector8_toUint64(vec);
}

export circuit toBytes(vec: Vector<8, Uint<8>>): Bytes<8> {
  toImpure = true;
  return Vector8_toBytes(vec);
}
